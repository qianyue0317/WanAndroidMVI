<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.WanAndroidMVI" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/green_light</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/teal_200</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimary</item>
        <!-- Customize your theme here. -->
    </style>

    <declare-styleable name="test">
        <attr name="indexColor" format="color"/>
    </declare-styleable>

    <style name="WanArticleTitle">
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">16sp</item>
    </style>
    
    <declare-styleable name="RoundedCornerLayout">
        <!--   采用outline方案实现圆角     -->
        <attr name="roundOutLineRadius" format="dimension" />

        <!--    采用clip canvas方式实现，目前没有必要实现    -->
        <attr name="roundClipRadius" format="dimension" />
        <attr name="roundClipColor" format="color" />
    </declare-styleable>
</resources>